"use client";

/**
 * DataPreloader Component
 * Previously: Pre-loaded and cached data for offline use
 * Now: No-op component (offline functionality removed)
 */
export function DataPreloader() {
  // No longer preloading data - offline functionality removed
  return null;
}
