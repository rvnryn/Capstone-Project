"use client";

/**
 * PagePreloader Component
 * Previously: Cached all pages for offline access
 * Now: No-op component (offline functionality removed)
 */
export function PagePreloader() {
  // No longer preloading pages - offline functionality removed
  return null;
}
